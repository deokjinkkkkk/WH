<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.great">


	<select id="greatCheck" resultType="int">
		SELECT count(*)
		FROM GREAT
		WHERE GREAT_N_CODE = #{great_n_code}
		AND ID = #{id}
	</select>

	<insert id="greatUP">
	INSERT INTO GREAT(GREAT_CODE, GREAT_N_CODE, id)
	VALUES((SELECT * FROM (SELECT MAX(GREAT_N_CODE)+1 FROM GREAT) NEXT),#{GREAT_N_CODE},#{id})
	</insert>

	<delete id="greatDown">
	DELETE FROM GREAT 
	WHERE GREAT_N_CODE = #{greatNcode} AND id = #{id}
	</delete>
	
	<select id="greatTotal">
	SELECT COUNT(*) 
	FROM GREAT
	WHERE GREAT_N_CODE = #{greatNcode}
	</select>
	
</mapper>