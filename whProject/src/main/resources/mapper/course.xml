<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.admin.wh.trip.mapper.CourseMapper">

	<select id="courseList" resultType="co.admin.wh.trip.vo.CourseVO">
	<!-- 페이징 시도, 이미지가 없는 데이터는 출력하지 않는다. -->
		SELECT * FROM COURSE
		WHERE TRIP_CODE = '0'
		ORDER BY COU_CODE DESC
	</select>
	
	<!-- api DB 등록 -->
	<insert id="insertInfo">
		INSERT INTO COURSE 		
		VALUES(#{couCode}, #{tripCode}, #{couName}, #{couDist}, #{sido}, #{time}, #{couContent}, 
			#{couRegDate}, #{couModDate}, #{couLat}, #{couLon}, #{imgGroCode}, #{regionCode}, #{contentId})
	</insert>
	
	<!-- 전체 건수 조회 -->
	<select id="getCountTotal" parameterType="co.admin.wh.trip.vo.TripSearchVO" resultType="int">
		SELECT COUNT(*) FROM COURSE	
		WHERE TRIP_CODE = '0'
	</select>
	
	<!-- 상세페이지 보기 -->
	<select id="detailSelect"
		resultType="co.admin.wh.trip.vo.CourseVO">
		SELECT c.cou_name, t.trip_addr, t.trip_lat, t.trip_lon, t.img_gro_code
		FROM course c, trip t 
		WHERE c.trip_code = t.trip_code
	</select>


</mapper>